# Extract the arithmetic operation functions out of calc.py into a new module ope…

**Tool:** `task`
**Severity:** unspecified

## What's wrong

Extract the arithmetic operation functions out of calc.py into a new module operations.py, and have calc.py import them from there so behaviour is unchanged. Pure refactor: no new dependencies.
